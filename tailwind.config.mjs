/**
 * Tailwind CSS v4 loads theme from CSS (`app/globals.css`: `@import` + `@theme`).
 * This file is wired via `@config` so editors and tooling recognize the project.
 */
/** @type {import('tailwindcss').Config} */
const config = {};

export default config;
